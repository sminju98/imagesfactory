# imagesfactory 프로젝트 기획서

## 📋 프로젝트 개요

**프로젝트명**: imagesfactory  
**목적**: AI 프롬프트 기반 대량 이미지 생성 및 이메일 전송 서비스  
**타겟 유저**: 디자이너, 마케터, 콘텐츠 크리에이터, 일반 사용자

## 🎯 핵심 기능

### 1. 인증 시스템
- 이메일/비밀번호 회원가입 및 로그인
- 구글 소셜 로그인 (OAuth 2.0)
- 이메일 인증
- 비밀번호 재설정
- JWT 기반 세션 관리

### 2. 포인트 시스템
- 신규 가입 시 무료 포인트 지급 (예: 1,000 포인트)
- 이미지 생성 비용: 100 포인트/장
- 포인트 충전 시스템 (결제 연동)
- 포인트 사용 내역 조회
- 포인트 잔액 실시간 표시

### 3. 이미지 생성 시스템
- 프롬프트 입력 (한글/영문 지원)
- 생성할 이미지 수량 선택 (1-50장)
- 이미지 스타일/비율 옵션 선택
  - 스타일: Realistic, Anime, Art, 3D 등
  - 비율: 1:1, 16:9, 9:16, 4:3
- 생성 전 예상 소비 포인트 표시
- 비동기 배치 처리 (Queue 시스템)
- 생성 진행률 실시간 표시

### 4. 이메일 전송
- 이미지 생성 완료 시 자동 이메일 발송
- 이메일에 이미지 압축 파일(ZIP) 첨부 또는 다운로드 링크 제공
- 이메일 템플릿 디자인 (HTML)
- 생성된 프롬프트 정보 포함

### 5. 대시보드
- 포인트 잔액 표시
- 최근 생성한 이미지 갤러리
- 생성 히스토리 (날짜, 프롬프트, 이미지 수, 소비 포인트)
- 진행 중인 작업 상태 표시
- 다운로드 링크 관리

### 6. 관리자 페이지
- 사용자 관리
- 포인트 관리 (수동 지급/차감)
- 생성 통계 대시보드
- 시스템 모니터링

## 🛠 기술 스택

### Frontend
- **프레임워크**: Next.js 14+ (App Router)
- **언어**: TypeScript
- **스타일링**: Tailwind CSS
- **UI 컴포넌트**: shadcn/ui
- **상태 관리**: Zustand 또는 React Context
- **폼 관리**: React Hook Form + Zod
- **API 통신**: Axios 또는 fetch

### Backend
- **프레임워크**: Next.js API Routes 또는 Node.js + Express
- **언어**: TypeScript
- **ORM**: Prisma
- **인증**: NextAuth.js (Auth.js)
- **이미지 생성 API**: 
  - DALL-E 3 (OpenAI)
  - Stable Diffusion (Stability AI)
  - Midjourney API (대안)
- **이메일 서비스**: 
  - SendGrid
  - Resend
  - AWS SES

### Database
- **주 데이터베이스**: PostgreSQL
- **캐시**: Redis (세션, Queue)

### 인프라
- **호스팅**: Vercel 또는 AWS (EC2 + S3)
- **파일 스토리지**: AWS S3 또는 Cloudflare R2
- **Queue 시스템**: BullMQ + Redis
- **모니터링**: Sentry, Vercel Analytics

### 결제
- **PG사**: 
  - 토스페이먼츠
  - 스트라이프 (Stripe)

## 🗄 데이터베이스 설계

### User (사용자)
```typescript
{
  id: string (UUID)
  email: string (unique)
  password: string (hashed, nullable for social login)
  name: string
  provider: 'email' | 'google'
  providerId: string (nullable)
  emailVerified: boolean
  points: number (default: 1000)
  createdAt: DateTime
  updatedAt: DateTime
}
```

### ImageGeneration (이미지 생성 작업)
```typescript
{
  id: string (UUID)
  userId: string (foreign key)
  prompt: string
  promptTranslated: string (영문 번역본)
  imageCount: number
  style: string
  aspectRatio: string
  status: 'pending' | 'processing' | 'completed' | 'failed'
  pointsUsed: number
  progress: number (0-100)
  completedAt: DateTime (nullable)
  createdAt: DateTime
  updatedAt: DateTime
}
```

### Image (생성된 이미지)
```typescript
{
  id: string (UUID)
  generationId: string (foreign key)
  imageUrl: string (S3 URL)
  thumbnailUrl: string
  order: number
  createdAt: DateTime
}
```

### PointTransaction (포인트 거래 내역)
```typescript
{
  id: string (UUID)
  userId: string (foreign key)
  amount: number (양수: 충전, 음수: 사용)
  type: 'purchase' | 'usage' | 'refund' | 'bonus'
  description: string
  relatedGenerationId: string (nullable)
  balanceAfter: number
  createdAt: DateTime
}
```

### Payment (결제)
```typescript
{
  id: string (UUID)
  userId: string (foreign key)
  amount: number (원화)
  points: number
  status: 'pending' | 'completed' | 'failed' | 'cancelled'
  paymentMethod: string
  transactionId: string (PG사 거래 ID)
  createdAt: DateTime
  updatedAt: DateTime
}
```

## 🔌 API 설계

### 인증 API
- `POST /api/auth/register` - 회원가입
- `POST /api/auth/login` - 로그인
- `POST /api/auth/logout` - 로그아웃
- `GET /api/auth/me` - 현재 사용자 정보
- `POST /api/auth/verify-email` - 이메일 인증
- `POST /api/auth/reset-password` - 비밀번호 재설정

### 이미지 생성 API
- `POST /api/generate` - 이미지 생성 요청
- `GET /api/generate/:id` - 생성 작업 상태 조회
- `GET /api/generate/:id/images` - 생성된 이미지 목록
- `GET /api/generate/history` - 생성 히스토리

### 포인트 API
- `GET /api/points/balance` - 포인트 잔액 조회
- `GET /api/points/transactions` - 거래 내역 조회
- `POST /api/points/purchase` - 포인트 구매

### 사용자 API
- `GET /api/user/profile` - 프로필 조회
- `PUT /api/user/profile` - 프로필 수정
- `GET /api/user/stats` - 사용 통계

### 결제 API
- `POST /api/payment/create` - 결제 생성
- `POST /api/payment/confirm` - 결제 확인
- `POST /api/payment/webhook` - 결제 웹훅

## 🎨 UI/UX 페이지 구성

### Public Pages
1. **랜딩 페이지** (`/`)
   - 서비스 소개
   - 주요 기능 설명
   - 가격 정책
   - 샘플 이미지 갤러리
   - CTA: 무료로 시작하기

2. **로그인** (`/login`)
   - 이메일/비밀번호 로그인
   - 구글 로그인 버튼
   - 회원가입 링크
   - 비밀번호 찾기

3. **회원가입** (`/signup`)
   - 이메일/비밀번호 입력
   - 이용약관 동의
   - 구글 회원가입 옵션

### Protected Pages (로그인 필요)
4. **대시보드** (`/dashboard`)
   - 포인트 잔액 카드
   - 빠른 생성 폼
   - 최근 생성 이미지 그리드
   - 진행 중인 작업 상태

5. **이미지 생성** (`/create`)
   - 프롬프트 입력 (Large Textarea)
   - 생성 옵션 (수량, 스타일, 비율)
   - 예상 비용 계산
   - 생성 버튼
   - 진행률 표시

6. **히스토리** (`/history`)
   - 생성 작업 목록 (테이블 또는 카드)
   - 필터링 (날짜, 상태)
   - 각 작업 클릭 시 상세 페이지

7. **히스토리 상세** (`/history/:id`)
   - 프롬프트 정보
   - 생성된 이미지 갤러리
   - 개별 다운로드
   - 전체 ZIP 다운로드
   - 재생성 버튼

8. **포인트** (`/points`)
   - 현재 잔액
   - 포인트 패키지 선택
   - 결제 버튼
   - 거래 내역

9. **프로필** (`/profile`)
   - 사용자 정보 수정
   - 이메일 변경
   - 비밀번호 변경
   - 계정 연동 상태

10. **관리자** (`/admin`)
    - 사용자 관리
    - 통계 대시보드
    - 포인트 관리

## 🔄 사용자 플로우

### 신규 사용자 플로우
```
1. 랜딩 페이지 방문
2. "무료로 시작하기" 클릭
3. 회원가입 (이메일 또는 구글)
4. 이메일 인증 (이메일 가입 시)
5. 대시보드 진입 (1,000 포인트 지급)
6. 이미지 생성 페이지
7. 프롬프트 입력 및 옵션 선택
8. 생성 요청 (포인트 차감)
9. 진행률 표시 화면
10. 완료 시 이메일 수신
11. 히스토리에서 다운로드
```

### 이미지 생성 플로우
```
1. 사용자가 프롬프트 입력
2. 이미지 수량 및 옵션 선택
3. 포인트 잔액 확인
4. 부족 시 → 충전 페이지 이동
5. 충분 시 → 생성 요청
6. 백엔드에서 Queue에 작업 추가
7. Worker가 AI API 호출
8. 이미지 생성 및 S3 업로드
9. DB에 이미지 URL 저장
10. 진행률 업데이트 (WebSocket 또는 Polling)
11. 모든 이미지 생성 완료
12. ZIP 파일 생성 및 S3 업로드
13. 이메일 발송 (ZIP 링크 포함)
14. 상태를 'completed'로 변경
```

## 💳 포인트 정책

### 가격 정책
- **기본**: 이미지 1장 = 100 포인트
- **신규 가입 보너스**: 1,000 포인트 (10장 무료)

### 충전 패키지
- 1,000 포인트: ₩5,000 (10장)
- 3,000 포인트: ₩13,500 (30장) - 10% 할인
- 10,000 포인트: ₩40,000 (100장) - 20% 할인
- 30,000 포인트: ₩105,000 (300장) - 30% 할인

## 🚀 개발 우선순위

### Phase 1: MVP (4주)
1. **Week 1**: 프로젝트 셋업 및 인증
   - Next.js 프로젝트 생성
   - 데이터베이스 설계 및 Prisma 셋업
   - 이메일/구글 로그인 구현
   - 기본 레이아웃 및 라우팅

2. **Week 2**: 포인트 시스템 및 UI
   - 포인트 모델 구현
   - 대시보드 UI
   - 이미지 생성 페이지 UI
   - 히스토리 페이지 UI

3. **Week 3**: 이미지 생성 기능
   - AI API 연동 (DALL-E 또는 Stable Diffusion)
   - Queue 시스템 구축
   - 이미지 생성 및 저장
   - 진행률 추적

4. **Week 4**: 이메일 및 테스팅
   - 이메일 서비스 연동
   - ZIP 파일 생성 및 전송
   - 통합 테스팅
   - 버그 수정 및 최적화

### Phase 2: 결제 및 고도화 (2주)
- 결제 시스템 연동 (토스페이먼츠 또는 Stripe)
- 포인트 충전 기능
- 관리자 페이지
- 사용 통계 및 분석

### Phase 3: 추가 기능 (지속적)
- 이미지 편집 기능
- 프롬프트 템플릿 라이브러리
- 커뮤니티 갤러리 (공유 기능)
- 이미지 품질 옵션 (고화질 = 더 많은 포인트)
- 배치 프롬프트 (여러 프롬프트 동시 처리)
- 다국어 지원
- 모바일 앱

## 📊 예상 비용

### 개발 비용
- 개발자 1명 x 6주 (MVP + 결제)

### 운영 비용 (월간 예상)
- **호스팅**: Vercel Pro ($20) 또는 AWS (~$50-100)
- **데이터베이스**: Vercel Postgres ($20) 또는 AWS RDS (~$30)
- **Redis**: Upstash (~$10) 또는 AWS ElastiCache (~$20)
- **S3 스토리지**: ~$10-50 (트래픽 따라)
- **이메일**: SendGrid ($15) 또는 AWS SES (~$5)
- **AI API**: 사용량 기반
  - DALL-E 3: $0.04-0.08/image
  - Stable Diffusion: $0.002-0.01/image (더 저렴)
- **도메인**: ~$12/년
- **SSL 인증서**: Let's Encrypt (무료) 또는 Cloudflare (무료)

### 수익 모델
- 포인트 판매 수익
- 구독 모델 (월간 포인트 자동 충전)
- 기업용 플랜 (API 제공)

## 🔐 보안 고려사항

1. **인증/인가**
   - 비밀번호 해싱 (bcrypt)
   - JWT 토큰 만료 관리
   - CSRF 보호
   - Rate limiting

2. **데이터 보호**
   - 환경 변수 암호화
   - API 키 안전한 관리
   - S3 버킷 접근 제어
   - 사용자 데이터 암호화

3. **API 보호**
   - API Rate limiting
   - 프롬프트 필터링 (부적절한 콘텐츠)
   - 포인트 조작 방지
   - 중복 요청 방지

## 📈 성능 최적화

1. **이미지 처리**
   - 썸네일 자동 생성
   - CDN 사용 (CloudFront, Cloudflare)
   - WebP 포맷 변환
   - Lazy loading

2. **데이터베이스**
   - 인덱싱 최적화
   - 쿼리 최적화
   - 커넥션 풀링
   - 캐싱 전략 (Redis)

3. **프론트엔드**
   - 이미지 최적화 (next/image)
   - 코드 스플리팅
   - SSR/ISR 활용
   - 번들 사이즈 최적화

## 🎯 성공 지표 (KPI)

- **사용자 지표**
  - 일간/주간/월간 활성 사용자 (DAU/WAU/MAU)
  - 신규 가입자 수
  - 사용자 유지율

- **비즈니스 지표**
  - 생성된 이미지 수
  - 포인트 충전 전환율
  - 평균 사용자당 매출 (ARPU)
  - 이탈률

- **기술 지표**
  - 이미지 생성 성공률
  - 평균 생성 시간
  - API 응답 시간
  - 에러율

## 📝 다음 단계

1. ✅ 기획 검토 및 피드백
2. ⬜ 디자인 와이어프레임/목업 작성 (Figma)
3. ⬜ 기술 스택 최종 확정
4. ⬜ 개발 환경 셋업
5. ⬜ Phase 1 개발 시작

---

## 💡 추가 아이디어

- **프롬프트 개선 AI**: 사용자 프롬프트를 AI가 개선해주는 기능
- **스타일 프리셋**: 인기 스타일 원클릭 적용
- **이미지 변형**: 기존 이미지를 업로드하여 변형
- **팀 기능**: 여러 사용자가 포인트 공유
- **레퍼럴 프로그램**: 친구 초대 시 포인트 보상
- **구독 플랜**: 월 정액제로 무제한 또는 대량 생성

