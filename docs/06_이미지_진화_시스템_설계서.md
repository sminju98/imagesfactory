# ImageFactory 이미지 진화 시스템 설계서

## 1. 개요

ImageFactory의 핵심 기능인 **이미지 진화 시스템(Evolution System)**과 **선호 갤러리(Favorite Gallery)**를 설계합니다.

### 1.1 핵심 컨셉
- 유저가 좋아요한 이미지를 "선택(selection)"으로 활용
- 선택된 이미지를 레퍼런스로 다음 세대 이미지 생성
- 유전 알고리즘 방식의 이미지 진화

### 1.2 주요 기능
1. **1세대 생성**: 프롬프트 → 멀티모델 이미지 생성
2. **좋아요 선택**: 마음에 드는 이미지 선택
3. **진화 생성**: 선택된 이미지 기반 2세대, 3세대... 생성
4. **선호 갤러리**: 좋아요한 이미지 자동 보관

### 1.3 참고 이미지 규칙 ⭐
- **최대 선택 개수**: 8개
- **배수 생성 규칙**: 참고 이미지 N개 선택 시, 모델별 생성 개수는 N의 배수
  - 예: 3개 선택 → 모델당 3장, 6장, 9장... 중 선택
  - 예: 5개 선택 → 모델당 5장, 10장, 15장... 중 선택
- **이유**: 각 참고 이미지가 공평하게 영향을 미치도록 함

---

## 2. 폴더 구조

```
src/
├── app/
│   ├── evolution/                    # 진화 시스템 페이지
│   │   ├── page.tsx                  # 메인 진화 페이지
│   │   └── [taskId]/
│   │       └── page.tsx              # 특정 Task의 진화 뷰
│   │
│   ├── gallery/                      # 갤러리 페이지
│   │   ├── page.tsx                  # 메인 갤러리
│   │   ├── favorites/
│   │   │   └── page.tsx              # 선호 갤러리
│   │   └── evolutions/
│   │       └── page.tsx              # 진화 히스토리
│   │
│   └── api/
│       ├── evolution/
│       │   ├── create/route.ts       # 진화 생성 API
│       │   ├── prompt/route.ts       # 진화 프롬프트 생성 API
│       │   └── history/route.ts      # 진화 히스토리 조회
│       │
│       └── gallery/
│           ├── favorites/route.ts    # 선호 갤러리 CRUD
│           └── like/route.ts         # 좋아요 토글 API
│
├── components/
│   ├── evolution/
│   │   ├── ImageCard.tsx             # 이미지 카드 (좋아요, 코멘트)
│   │   ├── GenerationGrid.tsx        # 세대별 이미지 그리드
│   │   ├── EvolutionModal.tsx        # 진화 설정 모달
│   │   ├── EvolutionProgress.tsx     # 진화 진행 상태
│   │   ├── SelectedReferences.tsx    # 선택된 레퍼런스 표시
│   │   └── GenerationHistory.tsx     # 세대별 히스토리
│   │
│   └── gallery/
│       ├── GalleryGrid.tsx           # 갤러리 그리드
│       ├── ImageDetail.tsx           # 이미지 상세 모달
│       ├── FilterBar.tsx             # 필터 바
│       └── CommentSection.tsx        # 코멘트 섹션
│
├── lib/
│   ├── evolution.ts                  # 진화 로직
│   └── gallery.ts                    # 갤러리 유틸리티
│
├── store/
│   └── evolutionStore.ts             # 진화 상태 관리 (Zustand)
│
└── types/
    └── evolution.ts                  # 진화 관련 타입
```

---

## 3. 데이터베이스 구조 (Firestore)

### 3.1 Collections

#### `images` (이미지 메타데이터)
```typescript
interface ImageDocument {
  id: string;
  userId: string;
  taskId: string;
  jobId: string;
  
  // 기본 정보
  prompt: string;
  refinedPrompt: string;
  modelId: string;
  imageUrl: string;
  thumbnailUrl: string;
  
  // 진화 정보
  generation: number;           // 1, 2, 3...
  parentIds: string[];          // 부모 이미지 ID들
  evolutionHistory: EvolutionStep[];
  
  // 인터랙션
  isLiked: boolean;
  likesCount: number;
  comments: Comment[];
  
  // 메타데이터
  width: number;
  height: number;
  fileSize: number;
  styleTags: string[];
  
  createdAt: Timestamp;
  updatedAt: Timestamp;
}

interface EvolutionStep {
  step: string;                 // "gen1", "gen2"...
  prompt: string;
  refinedPrompt: string;
  modelId: string;
  parentIds: string[];
  createdAt: Timestamp;
}

interface Comment {
  id: string;
  userId: string;
  content: string;
  createdAt: Timestamp;
}
```

#### `favorites` (선호 갤러리)
```typescript
interface FavoriteDocument {
  id: string;
  userId: string;
  imageId: string;
  imageUrl: string;
  thumbnailUrl: string;
  prompt: string;
  modelId: string;
  generation: number;
  tags: string[];
  note: string;
  createdAt: Timestamp;
}
```

#### `evolutions` (진화 세션)
```typescript
interface EvolutionSession {
  id: string;
  userId: string;
  name: string;
  
  // 세대별 이미지 ID 목록
  generations: {
    [generationNum: string]: string[];  // { "1": [id1, id2], "2": [id3, id4] }
  };
  
  // 현재 선택된 이미지들
  selectedImageIds: string[];
  
  // 설정
  basePrompt: string;
  currentGeneration: number;
  totalImages: number;
  
  status: 'active' | 'completed' | 'archived';
  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

---

## 4. React 컴포넌트 구조

### 4.1 ImageCard 컴포넌트
```tsx
interface ImageCardProps {
  image: ImageDocument;
  onLike: (imageId: string) => void;
  onComment: (imageId: string, comment: string) => void;
  onSelect: (imageId: string) => void;
  isSelected: boolean;
  showEvolutionBadge: boolean;
}

// 기능:
// - 이미지 표시
// - 좋아요 버튼 (❤️)
// - 코멘트 버튼 (💬)
// - 선택 체크박스
// - 세대 배지 표시
// - 호버 시 메타데이터 표시
```

### 4.2 GenerationGrid 컴포넌트
```tsx
interface GenerationGridProps {
  generation: number;
  images: ImageDocument[];
  selectedIds: string[];
  onImageSelect: (imageId: string) => void;
  onImageLike: (imageId: string) => void;
}

// 기능:
// - 세대별 이미지 그리드 표시
// - 모델별 그룹핑 옵션
// - 다중 선택 지원
// - 드래그 선택
```

### 4.3 EvolutionModal 컴포넌트
```tsx
interface EvolutionModalProps {
  isOpen: boolean;
  onClose: () => void;
  selectedImages: ImageDocument[];
  onStartEvolution: (config: EvolutionConfig) => void;
}

interface EvolutionConfig {
  selectedImageIds: string[];           // 선택된 참고 이미지 (최대 8개)
  additionalPrompt: string;
  modelsConfig: {
    [modelId: string]: number;          // 모델별 생성 개수 (참고 이미지 개수의 배수)
  };
  evolutionStrength: number;            // 0.1 ~ 1.0
  referenceCount: number;               // 참고 이미지 개수 (1~8)
}

// ⭐ 배수 생성 규칙
// referenceCount = 선택된 참고 이미지 개수
// 모델별 생성 개수는 referenceCount의 배수만 허용
// 예: referenceCount = 3 → 모델당 3, 6, 9장 선택 가능

// 기능:
// - 선택된 레퍼런스 이미지 표시 (최대 8개)
// - 모델별 생성 개수 설정 (배수 단위 드롭다운)
// - 추가 프롬프트 입력
// - 진화 강도 설정
// - 예상 포인트 표시
```

### 4.3.1 배수 생성 로직
```typescript
// 참고 이미지 개수에 따른 허용 생성 개수 계산
function getAllowedCounts(referenceCount: number, maxPerModel: number = 20): number[] {
  const counts: number[] = [];
  for (let i = referenceCount; i <= maxPerModel; i += referenceCount) {
    counts.push(i);
  }
  return counts;
}

// 예시:
// getAllowedCounts(1) → [1, 2, 3, 4, 5, ..., 20]
// getAllowedCounts(3) → [3, 6, 9, 12, 15, 18]
// getAllowedCounts(5) → [5, 10, 15, 20]
// getAllowedCounts(8) → [8, 16]

// 각 참고 이미지가 몇 장씩 생성에 사용되는지 계산
function getImagesPerReference(totalCount: number, referenceCount: number): number {
  return totalCount / referenceCount;
}

// 예시: 9장 생성, 3개 참고 이미지 → 각 참고 이미지당 3장씩 생성
```

### 4.4 EvolutionProgress 컴포넌트
```tsx
interface EvolutionProgressProps {
  taskId: string;
  totalSteps: number;
  currentStep: number;
  status: 'prompt' | 'generating' | 'complete' | 'error';
}

// 진행 단계:
// 1. 프롬프트 재해석 (GPT)
// 2. 모델별 파라미터 적용
// 3. 이미지 생산
// 4. 메타데이터 저장
```

---

## 5. 좋아요 기반 유전 알고리즘 로직

### 5.1 Selection (선택)
```typescript
// 유저가 좋아요한 이미지들이 다음 세대의 "부모"가 됨
async function selectParents(userId: string, sessionId: string): Promise<ImageDocument[]> {
  const session = await getEvolutionSession(sessionId);
  const currentGen = session.currentGeneration;
  
  // 현재 세대에서 좋아요한 이미지들 조회
  const likedImages = await db.collection('images')
    .where('userId', '==', userId)
    .where('taskId', '==', sessionId)
    .where('generation', '==', currentGen)
    .where('isLiked', '==', true)
    .get();
  
  return likedImages.docs.map(doc => doc.data() as ImageDocument);
}
```

### 5.2 Crossover (교차)
```typescript
// 여러 부모 이미지의 특성을 결합한 새 프롬프트 생성
async function crossoverPrompts(parents: ImageDocument[]): Promise<string> {
  const parentPrompts = parents.map(p => p.refinedPrompt);
  const parentStyles = parents.flatMap(p => p.styleTags);
  
  const systemPrompt = `
    당신은 이미지 생성 프롬프트 전문가입니다.
    여러 부모 이미지의 프롬프트를 분석하고, 
    이들의 장점을 결합한 새로운 프롬프트를 생성하세요.
    
    부모 프롬프트들:
    ${parentPrompts.map((p, i) => `[부모 ${i+1}]: ${p}`).join('\n')}
    
    공통 스타일 태그: ${[...new Set(parentStyles)].join(', ')}
    
    새 프롬프트를 생성하세요. 부모들의 공통 특성은 유지하고,
    각자의 독특한 특성을 자연스럽게 결합하세요.
  `;
  
  const response = await openai.chat.completions.create({
    model: 'gpt-4o',
    messages: [{ role: 'system', content: systemPrompt }],
  });
  
  return response.choices[0].message.content;
}
```

### 5.3 Mutation (변이)
```typescript
// 프롬프트에 랜덤 변이 추가
function mutatePrompt(prompt: string, mutationRate: number = 0.1): string {
  const mutations = [
    'more vibrant colors',
    'softer lighting',
    'more dramatic composition',
    'enhanced details',
    'artistic interpretation',
    'cinematic feel',
    'dreamlike atmosphere',
  ];
  
  if (Math.random() < mutationRate) {
    const mutation = mutations[Math.floor(Math.random() * mutations.length)];
    return `${prompt}, ${mutation}`;
  }
  
  return prompt;
}
```

### 5.4 전체 진화 플로우
```typescript
async function evolve(
  userId: string,
  sessionId: string,
  config: EvolutionConfig
): Promise<string> {
  // 1. 부모 선택
  const parents = await selectParents(userId, sessionId);
  if (parents.length === 0) {
    throw new Error('진화할 이미지를 선택해주세요');
  }
  
  // 2. 프롬프트 교차
  const crossedPrompt = await crossoverPrompts(parents);
  
  // 3. 변이 적용
  const mutatedPrompt = mutatePrompt(crossedPrompt, config.evolutionStrength);
  
  // 4. 추가 프롬프트 결합
  const finalPrompt = config.additionalPrompt 
    ? `${mutatedPrompt}. ${config.additionalPrompt}`
    : mutatedPrompt;
  
  // 5. 새 세대 생성
  const session = await getEvolutionSession(sessionId);
  const nextGeneration = session.currentGeneration + 1;
  
  // 6. Task 생성 (기존 이미지 생성 시스템 활용)
  const taskId = await createEvolutionTask({
    userId,
    sessionId,
    prompt: finalPrompt,
    generation: nextGeneration,
    parentIds: parents.map(p => p.id),
    modelsConfig: config.modelsConfig,
    referenceImages: parents.map(p => p.imageUrl),
  });
  
  // 7. 세션 업데이트
  await updateEvolutionSession(sessionId, {
    currentGeneration: nextGeneration,
    selectedImageIds: [],
  });
  
  return taskId;
}
```

---

## 6. API 흐름

### 6.1 진화 생성 API
```typescript
// POST /api/evolution/create
export async function POST(request: NextRequest) {
  const { userId, sessionId, config } = await request.json();
  
  // 1. 인증 확인
  const user = await verifyUser(userId);
  
  // 2. 포인트 확인
  const totalPoints = calculateEvolutionPoints(config);
  if (user.points < totalPoints) {
    return NextResponse.json({ error: '포인트 부족' }, { status: 400 });
  }
  
  // 3. 진화 시작
  const taskId = await evolve(userId, sessionId, config);
  
  // 4. 포인트 차감
  await deductPoints(userId, totalPoints);
  
  return NextResponse.json({ success: true, taskId });
}
```

### 6.2 좋아요 토글 API
```typescript
// POST /api/gallery/like
export async function POST(request: NextRequest) {
  const { userId, imageId } = await request.json();
  
  const imageRef = db.collection('images').doc(imageId);
  const image = await imageRef.get();
  
  const isLiked = !image.data().isLiked;
  
  await imageRef.update({
    isLiked,
    likesCount: isLiked 
      ? FieldValue.increment(1) 
      : FieldValue.increment(-1),
    updatedAt: FieldValue.serverTimestamp(),
  });
  
  // 좋아요면 선호 갤러리에 추가
  if (isLiked) {
    await addToFavorites(userId, imageId);
  } else {
    await removeFromFavorites(userId, imageId);
  }
  
  return NextResponse.json({ success: true, isLiked });
}
```

---

## 7. ContentFactory 연동 데이터 포맷

```typescript
interface ContentFactoryInput {
  // 선택된 이미지들
  selectedImages: {
    id: string;
    url: string;
    thumbnailUrl: string;
    prompt: string;
    refinedPrompt: string;
    modelId: string;
    generation: number;
    styleTags: string[];
    metadata: {
      width: number;
      height: number;
      evolutionHistory: EvolutionStep[];
    };
  }[];
  
  // 사용자 입력
  userPrompt: string;
  
  // 스타일 정보 (이미지들에서 추출)
  styleTags: string[];
  
  // 진화 히스토리
  evolutionHistory: {
    sessionId: string;
    totalGenerations: number;
    bestImages: string[];  // 각 세대 최고 좋아요 이미지
  };
  
  // 콘텐츠 타입 설정
  contentTypes: {
    reels: boolean;
    comic: boolean;
    cardnews: boolean;
    banner: boolean;
    thumbnail: boolean;
    detail: boolean;
  };
}

// 갤러리에서 ContentFactory로 이미지 전송
async function sendToContentFactory(
  userId: string,
  selectedImageIds: string[]
): Promise<ContentFactoryInput> {
  const images = await getImagesByIds(selectedImageIds);
  
  // 스타일 태그 추출 및 병합
  const allTags = images.flatMap(img => img.styleTags);
  const uniqueTags = [...new Set(allTags)];
  
  // 진화 세션 정보 조회
  const sessionIds = [...new Set(images.map(img => img.taskId))];
  const sessions = await getEvolutionSessions(sessionIds);
  
  return {
    selectedImages: images.map(img => ({
      id: img.id,
      url: img.imageUrl,
      thumbnailUrl: img.thumbnailUrl,
      prompt: img.prompt,
      refinedPrompt: img.refinedPrompt,
      modelId: img.modelId,
      generation: img.generation,
      styleTags: img.styleTags,
      metadata: {
        width: img.width,
        height: img.height,
        evolutionHistory: img.evolutionHistory,
      },
    })),
    userPrompt: '',
    styleTags: uniqueTags,
    evolutionHistory: {
      sessionId: sessions[0]?.id || '',
      totalGenerations: Math.max(...images.map(i => i.generation)),
      bestImages: images.map(i => i.id),
    },
    contentTypes: {
      reels: true,
      comic: true,
      cardnews: true,
      banner: true,
      thumbnail: true,
      detail: true,
    },
  };
}
```

---

## 8. UI 모킹

### 8.1 진화 메인 페이지 레이아웃
```
┌─────────────────────────────────────────────────────────┐
│  🧬 이미지 진화 시스템                    [새 세션 시작]  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 📌 선택된 레퍼런스 (3개)              [진화 시작] │   │
│  │ ┌─────┐ ┌─────┐ ┌─────┐                         │   │
│  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │                         │   │
│  │ │ ❤️  │ │ ❤️  │ │ ❤️  │                         │   │
│  │ └─────┘ └─────┘ └─────┘                         │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ═══════════════════════════════════════════════════   │
│                                                         │
│  🔹 1세대 이미지 (20장)                                 │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐             │
│  │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ ...        │
│  │ ☐❤️ │ │ ☑️❤️ │ │ ☐🤍 │ │ ☑️❤️ │ │ ☐🤍 │             │
│  │ GPT │ │ MJ  │ │ SDXL│ │ Flux│ │Gemini│             │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘             │
│                                                         │
│  🔹 2세대 이미지 (15장)                                 │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐             │
│  │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ ...        │
│  │ ☐🤍 │ │ ☑️❤️ │ │ ☐🤍 │ │ ☐🤍 │ │ ☐🤍 │             │
│  │ GPT │ │ MJ  │ │ SDXL│ │ Flux│ │Gemini│             │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 8.2 진화 설정 모달
```
┌─────────────────────────────────────────────────────────┐
│  🧬 2세대 진화 설정                              [닫기]  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  📌 선택된 참고 이미지 (3/8개)                          │
│  ┌─────┐ ┌─────┐ ┌─────┐  ┌───────────────────────┐   │
│  │ 🖼️  │ │ 🖼️  │ │ 🖼️  │  │ + 이미지 추가 (5개 남음)│   │
│  │ MJ  │ │ GPT │ │ Flux│  │      (최대 8개)       │   │
│  │  ✕  │ │  ✕  │ │  ✕  │  └───────────────────────┘   │
│  └─────┘ └─────┘ └─────┘                               │
│                                                         │
│  ℹ️ 3개 선택 → 모델당 3의 배수로 생성됩니다            │
│                                                         │
│  ────────────────────────────────────────────────────  │
│                                                         │
│  📝 추가 프롬프트 (선택)                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 더 밝은 색상으로, 배경을 숲으로 변경...          │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  🎛️ 진화 강도: ████████░░ 80%                          │
│                                                         │
│  📊 모델별 생성 개수 (3의 배수):                        │
│  ┌────────────────────────────────────────────────┐    │
│  │ ☑️ Midjourney  [▼ 3장│6장│9장]  60pt×3=180pt  │    │
│  │ ☑️ GPT-Image   [▼ 3장│6장│9장]  10pt×3=30pt   │    │
│  │ ☑️ Gemini      [▼ 3장│6장│9장]   8pt×3=24pt   │    │
│  │ ☑️ Flux        [▼ 3장│6장│9장]   1pt×3=3pt    │    │
│  │ ☐ SDXL        [  비활성화  ]                  │    │
│  │ ☐ Ideogram    [  비활성화  ]                  │    │
│  └────────────────────────────────────────────────┘    │
│                                                         │
│  📈 생성 요약: 4개 모델 × 3장 = 총 12장                 │
│  💰 예상 비용: 237 포인트 (현재 잔액: 5,000pt)          │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │            🧬 2세대 진화 시작 (12장)             │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 8.2.1 참고 이미지 개수별 UI 변화
```
┌──────────────────────────────────────────────────────────┐
│ 참고 이미지 1개 선택 시:                                 │
│ 📊 모델별 생성 개수 (1의 배수 = 자유 선택):              │
│    [▼ 1장│2장│3장│4장│5장│...│20장]                     │
├──────────────────────────────────────────────────────────┤
│ 참고 이미지 3개 선택 시:                                 │
│ 📊 모델별 생성 개수 (3의 배수):                          │
│    [▼ 3장│6장│9장│12장│15장│18장]                       │
├──────────────────────────────────────────────────────────┤
│ 참고 이미지 5개 선택 시:                                 │
│ 📊 모델별 생성 개수 (5의 배수):                          │
│    [▼ 5장│10장│15장│20장]                               │
├──────────────────────────────────────────────────────────┤
│ 참고 이미지 8개 선택 시 (최대):                          │
│ 📊 모델별 생성 개수 (8의 배수):                          │
│    [▼ 8장│16장]                                         │
└──────────────────────────────────────────────────────────┘
```

### 8.3 진화 진행 모달
```
┌─────────────────────────────────────────────────────────┐
│  🧬 2세대 이미지 생성 중...                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│           ████████████████░░░░░░░░  65%                 │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ ✅ 1. 프롬프트 재해석 (GPT)                      │   │
│  │    "A mystical forest with vibrant colors..."   │   │
│  │                                                  │   │
│  │ ✅ 2. 모델별 파라미터 적용                       │   │
│  │    • Midjourney: --v 6.1 --ar 1:1               │   │
│  │    • GPT-Image: size=1024x1024                  │   │
│  │                                                  │   │
│  │ 🔄 3. 이미지 생산 중... (7/10)                   │   │
│  │    ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐            │   │
│  │    │ ✅  │ │ ✅  │ │ ✅  │ │ 🔄  │ ...        │   │
│  │    │ MJ  │ │ GPT │ │ GPT │ │ Flux│            │   │
│  │    └─────┘ └─────┘ └─────┘ └─────┘            │   │
│  │                                                  │   │
│  │ ⏳ 4. 메타데이터 저장                            │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ⏱️ 예상 남은 시간: 약 2분                              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 9. 구현 우선순위

### Phase 1: 기본 인프라 (1주)
1. ✅ Firestore 스키마 설정
2. ✅ 기본 타입 정의
3. ✅ 좋아요 API 구현
4. ✅ ImageCard 컴포넌트

### Phase 2: 진화 시스템 코어 (2주)
1. 진화 프롬프트 생성 API (GPT)
2. 진화 Task 생성 로직
3. EvolutionModal 컴포넌트
4. GenerationGrid 컴포넌트

### Phase 3: 갤러리 시스템 (1주)
1. 선호 갤러리 페이지
2. 필터링 및 검색
3. 코멘트 기능
4. 이미지 상세 뷰

### Phase 4: ContentFactory 연동 (3일)
1. 갤러리 → ContentFactory 데이터 전송
2. 스타일 태그 자동 추출
3. 히스토리 연동

---

## 10. 마이페이지 갤러리 탭 추가

### 10.1 마이페이지 탭 구조
```
┌──────────────────────────────────────────────────────────────┐
│  마이페이지                                                   │
├──────────────────────────────────────────────────────────────┤
│  [대시보드] [갤러리] [포인트] [히스토리] [결제내역] [설정]     │
│       ↑                                                      │
│    기존 탭    ↑ 신규 추가                                     │
└──────────────────────────────────────────────────────────────┘
```

### 10.2 갤러리 탭 UI
```
┌──────────────────────────────────────────────────────────────┐
│  📸 내 갤러리                                                 │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ [전체] [❤️ 좋아요] [📤 업로드] [🧬 진화]    🔍 검색      ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
│  필터: [모델 ▼] [기간 ▼] [태그 ▼]     정렬: [최신순 ▼]       │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  📊 총 156장 | ❤️ 좋아요 42장 | 📤 업로드 12장          │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐          │
│  │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │          │
│  │ ❤️  │ │ 📤  │ │ ❤️  │ │ 🧬2 │ │ 🧬3 │ │ ❤️  │          │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘          │
│                                                              │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐          │
│  │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │ │ 🖼️  │          │
│  │ 📤  │ │ ❤️  │ │ ❤️  │ │ 🧬1 │ │ 📤  │ │ ❤️  │          │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘          │
│                                                              │
│  선택된 이미지: 3개  [콘텐츠팩토리로 보내기] [진화 시작]      │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 10.3 갤러리 탭 타입
```
전체: 모든 이미지 (생성 + 업로드 + 좋아요)
❤️ 좋아요: 좋아요한 이미지만
📤 업로드: 유저가 직접 업로드한 레퍼런스 이미지
🧬 진화: 진화 시스템으로 생성된 이미지 (세대 표시)
```

---

## 11. 업로드 이미지 갤러리 자동 저장

### 11.1 업로드 이미지 저장 플로우
```
유저가 레퍼런스 이미지 업로드
        ↓
Firebase Storage에 저장
        ↓
Firestore 'uploadedImages' 컬렉션에 메타데이터 저장
        ↓
마이페이지 갤러리 "📤 업로드" 탭에 자동 표시
```

### 11.2 업로드 이미지 데이터 구조
```typescript
interface UploadedImage {
  id: string;
  userId: string;
  
  // 이미지 정보
  imageUrl: string;
  thumbnailUrl: string;
  originalFileName: string;
  
  // 메타데이터
  width: number;
  height: number;
  fileSize: number;
  mimeType: string;
  
  // 사용 정보
  usedInTasks: string[];        // 레퍼런스로 사용된 Task ID들
  usedCount: number;            // 사용 횟수
  
  // 분류
  tags: string[];
  note: string;
  
  // 타임스탬프
  uploadedAt: Timestamp;
  lastUsedAt: Timestamp;
}
```

### 11.3 업로드 시 자동 저장 로직
```typescript
// 메인 페이지 또는 진화 페이지에서 레퍼런스 이미지 업로드 시
async function handleReferenceUpload(file: File, userId: string): Promise<UploadedImage> {
  // 1. Firebase Storage에 업로드
  const storageRef = ref(storage, `uploads/${userId}/${Date.now()}_${file.name}`);
  const snapshot = await uploadBytes(storageRef, file);
  const imageUrl = await getDownloadURL(snapshot.ref);
  
  // 2. 썸네일 생성 (Cloud Function 또는 클라이언트)
  const thumbnailUrl = await generateThumbnail(imageUrl);
  
  // 3. 이미지 메타데이터 추출
  const dimensions = await getImageDimensions(file);
  
  // 4. Firestore에 저장
  const uploadedImage: UploadedImage = {
    id: generateId(),
    userId,
    imageUrl,
    thumbnailUrl,
    originalFileName: file.name,
    width: dimensions.width,
    height: dimensions.height,
    fileSize: file.size,
    mimeType: file.type,
    usedInTasks: [],
    usedCount: 0,
    tags: [],
    note: '',
    uploadedAt: serverTimestamp(),
    lastUsedAt: serverTimestamp(),
  };
  
  await db.collection('uploadedImages').doc(uploadedImage.id).set(uploadedImage);
  
  return uploadedImage;
}

// 이미지가 레퍼런스로 사용될 때 업데이트
async function markImageAsUsed(imageId: string, taskId: string): Promise<void> {
  const imageRef = db.collection('uploadedImages').doc(imageId);
  await imageRef.update({
    usedInTasks: FieldValue.arrayUnion(taskId),
    usedCount: FieldValue.increment(1),
    lastUsedAt: serverTimestamp(),
  });
}
```

---

## 12. 신규 가입 기본 포인트

### 12.1 기본 포인트 설정
```typescript
const SIGNUP_BONUS_POINTS = 100;  // 가입 시 기본 제공 포인트
```

### 12.2 가입 시 포인트 지급 로직
```typescript
// src/app/api/auth/signup/route.ts 또는 Firebase Auth 트리거
async function onUserSignup(user: User): Promise<void> {
  // 1. 사용자 문서 생성
  await db.collection('users').doc(user.uid).set({
    uid: user.uid,
    email: user.email,
    displayName: user.displayName || '',
    photoURL: user.photoURL || '',
    provider: user.providerData[0]?.providerId || 'password',
    points: SIGNUP_BONUS_POINTS,  // ⭐ 기본 100 포인트
    emailVerified: user.emailVerified,
    createdAt: serverTimestamp(),
    updatedAt: serverTimestamp(),
    stats: {
      totalGenerations: 0,
      totalImages: 0,
      totalEvolutions: 0,
    },
  });
  
  // 2. 포인트 트랜잭션 기록
  await db.collection('pointTransactions').add({
    userId: user.uid,
    type: 'bonus',
    amount: SIGNUP_BONUS_POINTS,
    balanceBefore: 0,
    balanceAfter: SIGNUP_BONUS_POINTS,
    description: '회원가입 보너스 포인트',
    createdAt: serverTimestamp(),
  });
  
  // 3. 환영 이메일 발송
  await sendWelcomeEmail(user.email, user.displayName || '사용자', SIGNUP_BONUS_POINTS);
}
```

---

## 13. 어드민 시스템 기능 제안

### 13.1 현재 어드민 기능 (기존)
- AI 크레딧 잔액 조회
- 모델별 마지막 성공 시간

### 13.2 추가 제안 기능

#### 📊 대시보드 탭 (신규)
```
┌──────────────────────────────────────────────────────────────┐
│  📊 관리자 대시보드                                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────┐│
│  │ 총 사용자   │ │ 오늘 가입   │ │ 활성 사용자 │ │ 총 매출 ││
│  │   1,234명   │ │    +12명    │ │   156명     │ │ ₩5.2M   ││
│  │ ↑ 5.2%     │ │ ↑ 23%      │ │ (24h 내)    │ │ ↑ 12%   ││
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────┘│
│                                                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────┐│
│  │ 총 생성 수  │ │ 오늘 생성   │ │ 평균 생성   │ │ 성공률  ││
│  │  45,678장   │ │   +523장    │ │ 12장/유저   │ │  94.2%  ││
│  │ ↑ 8.3%     │ │ ↑ 15%      │ │             │ │ ↑ 1.2%  ││
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────┘│
│                                                              │
│  📈 일별 생성량 그래프                                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │     ▄                                                  │ │
│  │   ▄ █ ▄                                    ▄ ▄        │ │
│  │ ▄ █ █ █ ▄   ▄       ▄ ▄   ▄ ▄ ▄ ▄ ▄ ▄ ▄ █ █ ▄ ▄    │ │
│  │ █ █ █ █ █ ▄ █ ▄ ▄ ▄ █ █ ▄ █ █ █ █ █ █ █ █ █ █ █ ▄  │ │
│  │ ─────────────────────────────────────────────────────│ │
│  │ 11/1        11/7        11/14       11/21       11/28│ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

#### 👥 사용자 관리 탭 (신규)
```
┌──────────────────────────────────────────────────────────────┐
│  👥 사용자 관리                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  🔍 검색: [이메일/이름으로 검색________________] [검색]      │
│                                                              │
│  필터: [전체 ▼] [가입일 ▼] [포인트 ▼] [생성 수 ▼]           │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 이메일              │ 이름   │ 포인트 │ 생성 │ 가입일  │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ user1@email.com     │ 김철수 │ 5,230  │ 156  │ 11/01  │ │
│  │ user2@email.com     │ 이영희 │ 1,200  │ 45   │ 11/05  │ │
│  │ user3@email.com     │ 박민수 │ 8,500  │ 234  │ 10/28  │ │
│  │ ...                                                    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  선택된 사용자: 2명                                          │
│  [포인트 지급] [포인트 차감] [이메일 발송] [계정 정지]        │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

#### 💰 매출/결제 관리 탭 (신규)
```
┌──────────────────────────────────────────────────────────────┐
│  💰 매출 관리                                                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  기간: [2024-11-01] ~ [2024-11-28]  [조회]                   │
│                                                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │
│  │ 총 매출     │ │ 결제 건수   │ │ 평균 결제액 │            │
│  │ ₩5,234,000 │ │    234건    │ │  ₩22,370   │            │
│  └─────────────┘ └─────────────┘ └─────────────┘            │
│                                                              │
│  📊 결제수단별 분포                                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 카드결제 ████████████████████████████████ 78%          │ │
│  │ 계좌이체 ████████████ 18%                              │ │
│  │ 간편결제 ████ 4%                                       │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  최근 결제 내역:                                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 일시       │ 사용자    │ 금액    │ 포인트 │ 상태      │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ 11/28 14:23│ kim@...   │ ₩50,000│ 5,500  │ ✅ 완료   │ │
│  │ 11/28 13:45│ lee@...   │ ₩10,000│ 1,000  │ ✅ 완료   │ │
│  │ 11/28 12:30│ park@...  │ ₩30,000│ 3,300  │ ⏳ 대기   │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  [엑셀 다운로드] [환불 처리]                                 │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

#### 🖼️ 콘텐츠 모니터링 탭 (신규)
```
┌──────────────────────────────────────────────────────────────┐
│  🖼️ 콘텐츠 모니터링                                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ⚠️ 최근 실패한 생성 (24시간)                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Task ID     │ 사용자    │ 모델     │ 실패 사유         │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ abc123...   │ user@...  │ MJ       │ API 타임아웃      │ │
│  │ def456...   │ kim@...   │ Ideogram │ 크레딧 부족       │ │
│  │ ghi789...   │ lee@...   │ Leonardo │ 콘텐츠 정책 위반  │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  📊 모델별 성공률 (7일)                                      │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ GPT-Image  ████████████████████████████████████ 98.5%  │ │
│  │ Midjourney ██████████████████████████████████ 96.2%    │ │
│  │ Flux       ████████████████████████████████ 94.8%      │ │
│  │ Ideogram   ██████████████████████████████ 92.1%        │ │
│  │ Leonardo   ████████████████████████████ 89.3%          │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  🚨 신고된 콘텐츠                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 이미지 ID   │ 신고 사유      │ 신고일   │ 상태        │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ img001...   │ 부적절한 콘텐츠│ 11/27    │ 🔍 검토 중  │ │
│  │ img002...   │ 저작권 침해    │ 11/26    │ ✅ 처리완료 │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

#### ⚙️ 시스템 설정 탭 (신규)
```
┌──────────────────────────────────────────────────────────────┐
│  ⚙️ 시스템 설정                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  💰 포인트 설정                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 가입 보너스 포인트:  [100    ] pt                      │ │
│  │ 이메일 인증 보너스:  [50     ] pt                      │ │
│  │ 첫 결제 보너스:      [10     ] % 추가                  │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  🎨 모델별 포인트 단가                                       │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Midjourney:    [60    ] pt/장                          │ │
│  │ GPT-Image:     [10    ] pt/장                          │ │
│  │ Gemini:        [8     ] pt/장                          │ │
│  │ Flux:          [1     ] pt/장                          │ │
│  │ ...                                                    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  📧 이메일 설정                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 생성 완료 알림:      [✅ 활성화]                        │ │
│  │ 결제 확인 알림:      [✅ 활성화]                        │ │
│  │ 마케팅 이메일:       [☐ 비활성화]                       │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  🔧 시스템 유지보수                                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ [캐시 초기화] [타임아웃 Task 정리] [로그 다운로드]     │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  [변경사항 저장]                                             │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

#### 📢 공지/프로모션 관리 탭 (신규)
```
┌──────────────────────────────────────────────────────────────┐
│  📢 공지/프로모션 관리                                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  📌 현재 활성 공지                                           │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ✅ 서버 점검 안내 (11/30 02:00-04:00)                  │ │
│  │ ✅ 블랙프라이데이 50% 할인 (~11/30)                    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  [+ 새 공지 작성]                                            │
│                                                              │
│  🎁 프로모션 코드 관리                                       │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 코드        │ 혜택        │ 사용/한도  │ 만료일      │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ WELCOME2024 │ 500pt 지급  │ 45/100    │ 12/31       │ │
│  │ BLACKFRI    │ 50% 할인    │ 234/500   │ 11/30       │ │
│  │ VIPONLY     │ 1000pt 지급 │ 12/50     │ 12/15       │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  [+ 새 프로모션 코드 생성]                                   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 13.3 어드민 기능 요약

| 탭 | 기능 | 우선순위 |
|---|---|---|
| 📊 대시보드 | 핵심 지표 모니터링, 그래프 | ⭐⭐⭐ |
| 💳 AI 크레딧 | API 잔액, 상태 확인 | ⭐⭐⭐ (기존) |
| 👥 사용자 관리 | 검색, 포인트 지급/차감, 계정 관리 | ⭐⭐⭐ |
| 💰 매출 관리 | 결제 내역, 통계, 환불 처리 | ⭐⭐⭐ |
| 🖼️ 콘텐츠 모니터링 | 실패 로그, 신고 관리 | ⭐⭐ |
| ⚙️ 시스템 설정 | 포인트 단가, 이메일 설정 | ⭐⭐ |
| 📢 공지/프로모션 | 공지사항, 프로모션 코드 | ⭐ |

---

## 14. 예상 파일 목록

```
신규 생성 파일:

# 진화 시스템
├── src/app/evolution/page.tsx
├── src/app/evolution/[taskId]/page.tsx
├── src/app/api/evolution/create/route.ts
├── src/app/api/evolution/prompt/route.ts
├── src/components/evolution/ImageCard.tsx
├── src/components/evolution/GenerationGrid.tsx
├── src/components/evolution/EvolutionModal.tsx
├── src/components/evolution/EvolutionProgress.tsx
├── src/components/evolution/SelectedReferences.tsx
├── src/lib/evolution.ts
├── src/store/evolutionStore.ts
├── src/types/evolution.ts

# 갤러리 시스템
├── src/app/gallery/page.tsx
├── src/app/gallery/favorites/page.tsx
├── src/app/api/gallery/like/route.ts
├── src/app/api/gallery/favorites/route.ts
├── src/app/api/gallery/upload/route.ts           # 업로드 이미지 저장 API
├── src/components/gallery/GalleryGrid.tsx
├── src/components/gallery/GalleryTab.tsx          # 마이페이지 갤러리 탭
├── src/components/gallery/ImageDetail.tsx
├── src/components/gallery/FilterBar.tsx
├── src/components/gallery/UploadedImageCard.tsx   # 업로드 이미지 카드
├── src/lib/gallery.ts

# 어드민 시스템 확장
├── src/app/admin/dashboard/page.tsx               # 대시보드 탭
├── src/app/admin/users/page.tsx                   # 사용자 관리 탭
├── src/app/admin/revenue/page.tsx                 # 매출 관리 탭
├── src/app/admin/content/page.tsx                 # 콘텐츠 모니터링 탭
├── src/app/admin/settings/page.tsx                # 시스템 설정 탭
├── src/app/admin/promotions/page.tsx              # 공지/프로모션 탭
├── src/app/api/admin/dashboard/route.ts           # 대시보드 데이터 API
├── src/app/api/admin/users/route.ts               # 사용자 관리 API
├── src/app/api/admin/users/[id]/points/route.ts   # 포인트 지급/차감 API
├── src/app/api/admin/revenue/route.ts             # 매출 데이터 API
├── src/app/api/admin/promotions/route.ts          # 프로모션 관리 API
├── src/app/api/admin/settings/route.ts            # 시스템 설정 API
├── src/components/admin/DashboardChart.tsx        # 대시보드 차트
├── src/components/admin/UserTable.tsx             # 사용자 테이블
├── src/components/admin/RevenueChart.tsx          # 매출 차트
├── src/components/admin/PromotionForm.tsx         # 프로모션 폼

# 수정 파일
├── src/app/mypage/page.tsx                        # 갤러리 탭 추가
├── src/app/api/auth/signup/route.ts               # 가입 보너스 포인트 로직
├── src/app/page.tsx                               # 레퍼런스 업로드 시 저장 로직
```

---

## 15. 구현 우선순위 (업데이트)

### Phase 1: 기본 인프라 (1주)
1. ✅ Firestore 스키마 설정 (images, favorites, uploadedImages)
2. ✅ 기본 타입 정의
3. ✅ 좋아요 API 구현
4. ✅ ImageCard 컴포넌트
5. ⭐ 업로드 이미지 자동 저장 로직
6. ⭐ 가입 시 100포인트 지급 로직

### Phase 2: 마이페이지 갤러리 (3일)
1. 마이페이지에 갤러리 탭 추가
2. 전체/좋아요/업로드/진화 필터
3. 이미지 선택 및 액션 버튼

### Phase 3: 진화 시스템 코어 (2주)
1. 진화 프롬프트 생성 API (GPT)
2. 진화 Task 생성 로직 (배수 규칙 적용)
3. EvolutionModal 컴포넌트
4. GenerationGrid 컴포넌트

### Phase 4: 갤러리 시스템 완성 (1주)
1. 선호 갤러리 페이지
2. 필터링 및 검색
3. 코멘트 기능
4. 이미지 상세 뷰

### Phase 5: ContentFactory 연동 (3일)
1. 갤러리 → ContentFactory 데이터 전송
2. 스타일 태그 자동 추출
3. 히스토리 연동

### Phase 6: 어드민 시스템 확장 (1주)
1. 대시보드 탭 (핵심 지표)
2. 사용자 관리 탭
3. 매출 관리 탭
4. 시스템 설정 탭

---

이 설계서를 기반으로 단계별 구현을 진행합니다.

ㄱ